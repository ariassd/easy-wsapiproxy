# Project <%= docJson.info.name %>

## Description: <%= docJson.info.description || "" %>

<% docJson.item.forEach((item) => { %>
  # 🗂 Collection: <%=item.name %> 
  <% if (item.item) { %>
    <% item.item.forEach((method) => { %>
      ## Endpoint <%= method.name %>
      ### Description: <%= method.request.description || "" %>
      Method: <%= method.request.method %>
      >```
      ><%= unescape(method.request.url.raw) %>
      >```
      
      <% if (method.request.header && method.request.header.length >= 1) { %>
        ### Headers
        |Content-Type|Value|
        |---|---|
        <% method.request.header.forEach((header) => { -%>
          |<%= header.key %> | <%= header.value %>| <% -%>
        <% }) %>
      <% } %>
      <% if (method.request.body && method.request.body.mode == 'raw') { %>
        ### Body <%=method.request.body.mode%>
        ```json
          <% method.request.body.raw.split('\n').forEach((i, ix) => { -%>
            <%- `${ix+1}  ${i}` %>
          <% }) -%>
        ```
      <% } %>
      <% if (method.request.body && method.request.body.mode == 'formdata') { %>
        ### Body <%=method.request.body.mode%>
        
        |Param|value|Type|
        |---|---|---| 
        <% method.request.body.formdata.forEach((form) => { -%>
          |<%=form.key %>|<%=form.type === "file" ? form.src : form.value !== undefined ? form.value.replace(/\\n/g, "") : ""%>|<%=form.type%>| <% -%>
        <% }) %>
      <% } %>
      <% if (method.request.url.query && method.request.url.query.length >= 1) { %>
        ### Query Params
        |Param|value| 
        |---|---| <% -%>
        <% method.request.url.query.forEach((query) => { -%> 
          |<%=query.key %>|<%=query.value -%>| <% -%>
        <% }) -%>
      <% } %>
      <% if ( method.request.auth ) { %>
        ### 🔑 Authentication <%= method.request.auth.type %>
        <% if ( method.request.auth && method.request.auth.bearer ) { %>
          |Param|value|Type|
          |---|---|---| 
          <% method.request.auth.bearer.map( (auth) => { %>
            |<%=auth.key %>|<%=auth.value %>|<%=auth.type %>| <% -%>
          <% }) %>
        <% } %>
      <% } %>
      ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃ ⁃
    <% }) %>
  <% } %>

<% }) %>

_________________________________________________

Made with [easy-postman2md](https://github.com/ariassd/easy-postman2md) 
